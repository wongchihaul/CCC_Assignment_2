{
  "_id": "_design/testAurin",
  "views": {
      "viewname": {
          "map": "function(doc) { emit(doc._id, doc.for_rent_home_lease_minimumprice); }",
          "reduce": "function(keys, values) { return sum(values); }"
      }
  }
}

